# ChronoGate
ChronoGate is an API service that uses Chrono’s pluggable rate-limiting engine to enforce per-client traffic policies with configurable algorithms, real-time limit headers, and predictable overload protection.
